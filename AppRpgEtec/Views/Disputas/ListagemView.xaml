<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AppRpgEtec.Views.Disputas.ListagemView"
             Title="Disputas">

    <VerticalStackLayout Padding="10" Spacing="10">

        <SearchBar x:Name="searchBar"
                   HeightRequest="40"
                   Placeholder="Digite o nome do Personagem..."
                   TextColor="Black"
                   SearchCommand="{Binding PesquisarPersonagensCommand}"
                   SearchCommandParameter="{Binding Source={x:Reference searchBar}, Path=Text}" />

        <Entry Placeholder="Digite o Nome do Personagem..."
               Text="{Binding TextoBuscaDigitado}" />

        <ListView x:Name="lvPersonagens"
                  HasUnevenRows="True"
                  ItemsSource="{Binding PersonagensEncontrados}"
                  SelectedItem="{Binding PersonagemSelecionado}"
                  Margin="10,5,0,0">

            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout Orientation="Vertical">
                            <Label Text="{Binding Id}"
                                   TextColor="Blue"
                                   IsVisible="False" />

                            <Label Text="{Binding Nome}"
                                   TextColor="Blue"
                                   FontSize="18" />

                            <Label Text="{Binding Classe}"
                                   TextColor="Blue"
                                   FontSize="14" />
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>

        </ListView>

        <Grid HorizontalOptions="Fill"
              Margin="5,5,0,0"
              Padding="10,10,0,0">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Label Text="Atacante: "
                   Grid.Column="0"
                   Grid.Row="0"
                   FontAttributes="Bold" />

            <Label Text="{Binding DescricaoPersonagemAtacante}"
                   Grid.Column="1"
                   Grid.Row="0"
                   TextColor="Blue" />

            <Label Text="Oponente: "
                   Grid.Column="0"
                   Grid.Row="1"
                   FontAttributes="Bold" />

            <Label Text="{Binding DescricaoPersonagemOponente}"
                   Grid.Column="1"
                   Grid.Row="1"
                   TextColor="Blue" />

            <Button Text="Disputar com Arma"
                    Grid.Column="0"
                    Grid.Row="2"
                    Grid.ColumnSpan="2"
                    FontAttributes="Bold"
                    Command="{Binding DisputaComArmaCommand}" />

            <Picker Title="---Selecione a habilidade do primeiro combatente---"
                    Grid.Column="0"
                    Grid.Row="3"
                    Grid.ColumnSpan="2"
                    ItemsSource="{Binding Habilidades}"
                    ItemDisplayBinding="{Binding HabilidadeNome}"
                    SelectedItem="{Binding HabilidadeSelecionada}" />

            <Button Text="Disputar com Habilidade"
                    Grid.Column="0"
                    Grid.Row="4"
                    Grid.ColumnSpan="2"
                    FontAttributes="Bold"
                    Command="{Binding DisputaComHabilidadeCommand}" />

            <Button Text="Disputa Geral"
                    Grid.Column="0"
                    Grid.Row="5"
                    Grid.ColumnSpan="2"
                    FontAttributes="Bold"
                    Command="{Binding DisputaGeralCommand}" />

        </Grid>

    </VerticalStackLayout>
</ContentPage>